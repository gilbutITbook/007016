def repeat(s: str, n: int) -> str:
    """s를 n번 반복해서 반환하되 n이 음수면 빈 문자열을 반환합니다. 

    >>> repeat('yes', 4)
    'yesyesyesyes'
    >>> repeat('no', 0)

    >>> repeat('no', -2)

    >>> repeat('yesnomaybe', 3)

    """
