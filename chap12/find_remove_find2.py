def find_two_smallest(L):
    """ (앞 코드 참고) """
    
    # L에서 가장 작은 항목을 구한다. <- 새롭게 추가된 행
    # 가장 작은 항목의 인덱스를 구한다. <- 새롭게 추가된 행
    # 리스트에서 그 항목을 삭제한다.
    # 리스트에서 다음으로 가장 작은 항목의 인덱스를 구한다.
    # 가장 작은 항목을 리스트에 다시 넣는다.
    # 필요에 따라 두 번째 인덱스를 조정한다.
    # 두 인덱스를 반환한다.