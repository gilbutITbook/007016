def above_freezing_v2(celsius: float) -> bool:
    """celsius가 어는점보다 높으면 True를 반환한다. 

    >>> above_freezing_v2(5.2)
    True
    >>> above_freezing_v2(-2)
    False
    """

    return celsius >= 0
